{
  "name": "unseen",
  "version": "0.2.0",
  "private": true,
  "description": "End-to-end encrypted one-on-one chat over a Bun relay",
  "keywords": [
    "bun",
    "chat",
    "e2ee",
    "end-to-end-encryption",
    "ephemeral",
    "lit",
    "opfs",
    "prf",
    "web-platform",
    "webauthn"
  ],
  "homepage": "https://unseen.fyi",
  "license": "MIT",
  "author": {
    "name": "Alexey Simonenko",
    "email": "alexey@simonenko.dev",
    "url": "http://simonenko.xyz"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/meritt/unseen.fyi.git"
  },
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "type": "module",
  "scripts": {
    "ci": "bun run format:check && bun run lint && bun run typecheck && bun run audit && bun run test && bun run perf:bundle-size",
    "audit": "bun audit --prod && bun audit --audit-level=high",
    "lint": "oxlint --type-aware",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "typecheck": "tsc --noEmit -p shared/tsconfig.json && tsc --noEmit -p client/tsconfig.json && tsc --noEmit -p server/tsconfig.json && tsc --noEmit -p tsconfig.json",
    "test": "bun test shared client server --preload ./client/test/_helpers/dom-globals.ts --parallel",
    "test:e2e": "playwright test --config=test/playwright.config.ts",
    "build:client": "bun run --cwd client build.ts",
    "dev:server": "BUN_FEATURE_FLAG_NO_ORPHANS=1 UNSEEN_ALLOWED_ORIGINS=http://localhost:5173 bun run server/src/index.ts",
    "dev:client": "BUN_FEATURE_FLAG_NO_ORPHANS=1 bun run --cwd client dev-server.ts",
    "perf:bundle-size": "NODE_ENV=production UNSEEN_WS_URL=/ws bun run --cwd client build.ts && bun run test/perf/bundle-size.ts",
    "perf:lighthouse": "BUN_FEATURE_FLAG_NO_ORPHANS=1 bun run test/perf/lighthouse.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "1.63.0",
    "@types/bun": "1.4.2",
    "chrome-launcher": "^1.2.1",
    "lighthouse": "^13.4.1",
    "oxfmt": "0.67.0",
    "oxlint": "1.82.0",
    "oxlint-tsgolint": "7.0.2001",
    "typescript": "7.0.2"
  },
  "overrides": {
    "playwright-core": "1.63.0"
  },
  "engines": {
    "bun": ">=1.4.2"
  },
  "packageManager": "bun@1.4.2"
}
